# Supabase project (used by @nuxtjs/supabase — see layers/cms-core/nuxt.config.ts)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key

# Server-only: used solely by supabase/migrations tooling and any future
# admin scripts that must bypass RLS. Never expose this to the client and
# never reference it from Nitro API routes that serve user requests —
# those must use the request-scoped, RLS-enforced client (see
# layers/cms-core/server/utils/requireSiteAccess.ts).
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
